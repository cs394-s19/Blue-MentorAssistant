import React from 'react';
import { withStyles, } from '@material-ui/core';

const styles = {

};

const Home = () => {

  return (
    <div><p>This is a homepage.</p></div>
  );
}

export default withStyles(styles)(Home);
