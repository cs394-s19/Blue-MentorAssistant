import React from 'react';
import { withStyles, } from '@material-ui/core';

const styles = {

};

const NewTicket = () => {

  return (
    <div><p>This is the new ticket page.</p></div>
  );
}

export default withStyles(styles)(NewTicket);
